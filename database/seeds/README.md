# Database seeds

Only deterministic taxonomy and explicitly approved content are seeded. User, payment, and private organization data are never committed. The publication synchronizer will store the source revision and hash for every imported content version.


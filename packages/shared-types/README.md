# Shared types

Cross-language API types are generated from FastAPI OpenAPI into `packages/api-client`. This directory is reserved for non-API TypeScript domain utilities that have a single generated or canonical source.


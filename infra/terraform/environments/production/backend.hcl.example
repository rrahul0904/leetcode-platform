# Copy outside Git or pass with -backend-config. Production state must use a production-only bucket.
bucket       = "REPLACE_WITH_PRODUCTION_TERRAFORM_STATE_BUCKET"
key          = "rigor/production/terraform.tfstate"
region       = "us-east-1"
encrypt      = true
use_lockfile = true

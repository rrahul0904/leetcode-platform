# Manifests

The canonical complete planning manifest is `content/question-bank-manifest.json` to satisfy the repository contract. Batch manifests and immutable publication reports will live here; they must reference, not duplicate, the canonical entries.

